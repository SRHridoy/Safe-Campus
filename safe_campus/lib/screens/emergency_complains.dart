import 'package:flutter/material.dart';

class EmergencyComplainsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // You can replace this with your actual emergency complaints data and UI
    return Center(
      child: Text(
        'No emergency complaints yet.',
        style: TextStyle(fontSize: 18, color: Colors.redAccent),
      ),
    );
  }
}

